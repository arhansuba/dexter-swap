// src/lib.rs

// Bu alana gerekli modül ve kodlarınızı ekleyin.
// Örneğin:
// mod my_module;
// use crate::my_module::MyStruct;

// Bu dosya bir kütüphane olarak derlensinse genellikle burada bir trait yada struct tanımlamanız yapılır.

// Bu alanı güvende bırakın!
#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
